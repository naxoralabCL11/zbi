:root{
    --bg-void:#0a0618;
    --bg-deep:#150a2e;
    --pink:#ff2ea6;
    --purple:#a855f7;
    --cyan:#22e5ff;
    --mist:#f3ecff;
    --glass-fill: rgba(255,255,255,0.06);
    --glass-border: rgba(255,255,255,0.16);
    --font-display: 'Orbitron', sans-serif;
    --font-body: 'Space Grotesk', sans-serif;
  }

  *{ margin:0; padding:0; box-sizing:border-box; }

  html,body{
    width:100vw;
    height:100dvh;
    overflow:hidden;
    background:var(--bg-void);
    font-family:var(--font-body);
    color:var(--mist);
    cursor:none;
  }

  ::selection{ background:var(--pink); color:var(--bg-void); }

  /* ---------- animated space background ---------- */
  #sky{
    position:fixed; inset:0; z-index:0;
    background:
      radial-gradient(ellipse at 20% 30%, rgba(255,46,166,0.25), transparent 55%),
      radial-gradient(ellipse at 80% 70%, rgba(34,229,255,0.22), transparent 55%),
      radial-gradient(ellipse at 60% 20%, rgba(168,85,247,0.28), transparent 55%),
      linear-gradient(160deg, var(--bg-void), var(--bg-deep) 60%, #1a0a33 100%);
    background-size:200% 200%, 200% 200%, 200% 200%, 100% 100%;
    animation: driftSky 22s ease-in-out infinite alternate;
  }
  @keyframes driftSky{
    0%{ background-position: 0% 0%, 100% 100%, 50% 0%, 0 0; }
    100%{ background-position: 100% 60%, 0% 30%, 20% 80%, 0 0; }
  }
  #stars, #stars2{
    position:fixed; inset:0; z-index:1; pointer-events:none;
    background-repeat:repeat;
  }
  #stars{
    background-image:
      radial-gradient(1.5px 1.5px at 20px 30px, #fff, transparent),
      radial-gradient(1px 1px at 90px 120px, #fff, transparent),
      radial-gradient(1.5px 1.5px at 160px 60px, var(--cyan), transparent),
      radial-gradient(1px 1px at 210px 190px, #fff, transparent),
      radial-gradient(1.5px 1.5px at 260px 20px, var(--pink), transparent);
    background-size:280px 220px;
    opacity:.8;
    animation: twinkle 4s ease-in-out infinite alternate;
  }
  #stars2{
    background-image:
      radial-gradient(1px 1px at 40px 80px, #fff, transparent),
      radial-gradient(1.5px 1.5px at 130px 30px, var(--purple), transparent),
      radial-gradient(1px 1px at 190px 140px, #fff, transparent);
    background-size:220px 260px;
    opacity:.5;
    animation: twinkle 6s ease-in-out infinite alternate-reverse;
  }
  @keyframes twinkle{ 0%{ opacity:.3; } 100%{ opacity:.9; } }

  /* ---------- custom cursor ---------- */
  #cursor{
    position:fixed; top:0; left:0; width:22px; height:22px;
    border-radius:50%;
    background:radial-gradient(circle, rgba(255,46,166,.9), rgba(34,229,255,.5) 60%, transparent 75%);
    box-shadow:0 0 18px 4px rgba(255,46,166,.6), 0 0 34px 10px rgba(34,229,255,.3);
    pointer-events:none; z-index:9999;
    transform:translate(-50%,-50%);
    transition: width .18s ease, height .18s ease, background .18s ease, box-shadow .18s ease;
  }
  #cursor.grow{
    width:46px; height:46px;
    box-shadow:0 0 26px 8px rgba(255,46,166,.75), 0 0 50px 16px rgba(168,85,247,.4);
  }

  @media (hover:none), (pointer:coarse){
    html,body{ cursor:auto; }
    #cursor{ display:none; }
  }

  /* ---------- scroll snap frame ---------- */
  #frame{
    position:relative; z-index:2;
    width:100vw; height:100dvh;
    overflow-y:scroll; overflow-x:hidden;
    scroll-snap-type: y mandatory;
    scrollbar-width:none;
  }
  #frame::-webkit-scrollbar{ display:none; }

  section{
    width:100vw; height:100dvh;
    scroll-snap-align:start; scroll-snap-stop:always;
    display:flex; flex-direction:column; align-items:center; justify-content:center;
    position:relative;
    padding: clamp(20px, 4vw, 60px);
  }

  .eyebrow{
    font-family:var(--font-body); font-weight:600; letter-spacing:.35em;
    text-transform:uppercase; font-size:.7rem; color:var(--cyan);
    text-shadow:0 0 12px rgba(34,229,255,.7);
    margin-bottom:14px;
  }

  /* ---------- glass card ---------- */
  .glass{
    background:var(--glass-fill);
    border:1px solid var(--glass-border);
    backdrop-filter: blur(18px) saturate(140%);
    -webkit-backdrop-filter: blur(18px) saturate(140%);
    border-radius:20px;
    box-shadow: 0 8px 32px rgba(0,0,0,.35), inset 0 1px 0 rgba(255,255,255,.08);
    transition: transform .35s ease, box-shadow .35s ease, border-color .35s ease;
  }
  .glass:hover{
    transform: translateY(-6px);
    border-color: rgba(255,255,255,.35);
    box-shadow: 0 14px 40px rgba(0,0,0,.4), 0 0 0 1px rgba(255,46,166,.25), 0 0 40px rgba(168,85,247,.35);
  }

  /* ---------- glow button ---------- */
  .glow-btn{
    font-family:var(--font-display); font-weight:700; letter-spacing:.08em;
    text-transform:uppercase; font-size:.85rem;
    color:var(--bg-void);
    background:linear-gradient(135deg, var(--cyan), var(--pink) 55%, var(--purple));
    border:none; border-radius:999px;
    padding:16px 38px;
    cursor:none;
    box-shadow:0 0 20px rgba(255,46,166,.55), 0 0 40px rgba(34,229,255,.3);
    transition: transform .25s ease, box-shadow .25s ease;
  }
  .glow-btn:hover{
    transform: scale(1.06);
    box-shadow:0 0 34px rgba(255,46,166,.85), 0 0 64px rgba(34,229,255,.55);
  }
  .glow-btn:active{ transform: scale(.97); }

  .ghost-btn{
    font-family:var(--font-body); font-weight:600; font-size:.85rem; letter-spacing:.05em;
    color:var(--mist);
    background:transparent;
    border:1px solid var(--glass-border);
    border-radius:999px; padding:12px 26px;
    cursor:none;
    transition: border-color .25s ease, box-shadow .25s ease, color .25s ease;
  }
  .ghost-btn:hover{
    border-color:var(--cyan);
    color:var(--cyan);
    box-shadow:0 0 20px rgba(34,229,255,.4);
  }

  /* ---------- nav dots ---------- */
  #nav{
    position:fixed; right: clamp(14px,2.5vw,36px); top:50%; transform:translateY(-50%);
    z-index:50; display:flex; flex-direction:column; gap:16px;
  }
  #nav button{
    width:10px; height:10px; border-radius:50%;
    background:rgba(255,255,255,.25); border:1px solid rgba(255,255,255,.4);
    cursor:none; transition:all .3s ease; padding:0;
  }
  #nav button.active{
    background:var(--pink);
    box-shadow:0 0 12px 3px rgba(255,46,166,.8);
    transform:scale(1.3);
  }

  /* ================= HERO ================= */
  #hero{ text-align:center; }
  .planet-orb{
    width:min(38vh, 260px); height:min(38vh,260px);
    border-radius:50%;
    margin:0 auto 28px;
    background:
      radial-gradient(circle at 32% 30%, rgba(255,255,255,.55), transparent 40%),
      conic-gradient(from 200deg, var(--pink), var(--purple), var(--cyan), var(--pink));
    box-shadow: 0 0 60px 12px rgba(255,46,166,.45), 0 0 120px 30px rgba(34,229,255,.25), inset -20px -20px 60px rgba(0,0,0,.45);
    animation: spin 16s linear infinite, floaty 6s ease-in-out infinite;
  }
  @keyframes spin{ to{ filter:hue-rotate(360deg); } }
  @keyframes floaty{ 0%,100%{ transform:translateY(0);} 50%{ transform:translateY(-14px);} }

  h1.title{
    font-family:var(--font-display); font-weight:900;
    font-size:clamp(2.6rem, 8vw, 5.5rem);
    letter-spacing:.04em;
    background:linear-gradient(90deg, var(--cyan), var(--pink) 50%, var(--purple));
    -webkit-background-clip:text; background-clip:text; color:transparent;
    text-shadow:0 0 40px rgba(255,46,166,.25);
    line-height:1;
  }
  .tagline{
    font-size:clamp(1rem, 2vw, 1.25rem);
    color:rgba(243,236,255,.8);
    max-width:560px; margin:18px auto 34px;
    font-weight:400;
  }
  #hero .actions{ display:flex; gap:16px; flex-wrap:wrap; justify-content:center; }

  /* ================= GALLERY ================= */
  #gallery{ align-items:stretch; padding-top: clamp(60px,8vh,90px); }
  .section-head{ text-align:center; margin-bottom: clamp(20px,3.5vh,40px); }
  .section-head h2{
    font-family:var(--font-display); font-weight:700;
    font-size:clamp(1.6rem, 4vw, 2.6rem);
    color:var(--mist);
  }
  .section-head p{ color:rgba(243,236,255,.65); margin-top:8px; font-size:.95rem; }

  .gallery-grid{
    display:grid;
    grid-template-columns:repeat(auto-fit, minmax(180px, 1fr));
    gap:clamp(14px,2vw,22px);
    width:min(1100px, 92vw);
    max-height:60dvh;
    overflow:hidden;
  }
  .girl-card{
    padding:14px; text-align:center;
    display:flex; flex-direction:column; align-items:center; gap:10px;
  }
  .avatar{
    width:100%; aspect-ratio:1/1; border-radius:16px;
    display:flex; align-items:center; justify-content:center;
    font-family:var(--font-display); font-weight:700; font-size:1.8rem;
    color:rgba(255,255,255,.92);
    position:relative; overflow:hidden;
    box-shadow: inset 0 0 30px rgba(0,0,0,.35);
  }
  .avatar::after{
    content:''; position:absolute; inset:0;
    background:linear-gradient(160deg, rgba(255,255,255,.25), transparent 55%);
  }
  .girl-card h3{ font-family:var(--font-body); font-weight:600; font-size:.95rem; }
  .girl-card span{ font-size:.72rem; color:var(--cyan); letter-spacing:.05em; text-transform:uppercase; }

  /* ================= PLANET MAP ================= */
  #map{ padding-top:clamp(60px,8vh,90px); }
  .map-wrap{
    position:relative;
    width:min(720px, 88vw); aspect-ratio:1/1; max-height:56dvh;
    border-radius:50%;
    background:
      radial-gradient(circle at 35% 30%, rgba(255,46,166,.35), transparent 45%),
      radial-gradient(circle at 70% 65%, rgba(34,229,255,.3), transparent 45%),
      radial-gradient(circle at 50% 50%, #2a1150, #0d0620 75%);
    box-shadow:0 0 70px rgba(168,85,247,.4), inset 0 0 60px rgba(0,0,0,.55);
    border:1px solid rgba(255,255,255,.12);
  }
  .map-wrap svg{ position:absolute; inset:0; width:100%; height:100%; }
  .map-wrap .grid-line{ stroke:rgba(255,255,255,.12); stroke-width:1; fill:none; }

  .node{
    position:absolute; width:16px; height:16px; border-radius:50%;
    background:var(--cyan);
    box-shadow:0 0 10px 3px rgba(34,229,255,.9);
    transform:translate(-50%,-50%);
    cursor:none;
    animation: pulse 2.4s ease-in-out infinite;
  }
  .node:nth-child(odd){ background:var(--pink); box-shadow:0 0 10px 3px rgba(255,46,166,.9); }
  @keyframes pulse{ 0%,100%{ box-shadow:0 0 8px 2px currentColor; } 50%{ box-shadow:0 0 18px 8px currentColor; } }

  .node-label{
    position:absolute; transform:translate(-50%, -140%);
    font-size:.68rem; padding:8px 12px; white-space:nowrap;
    opacity:0; pointer-events:none; transition:opacity .25s ease, transform .25s ease;
    max-width:220px; white-space:normal; text-align:center;
  }
  .node-label strong{ display:block; font-family:var(--font-display); font-size:.75rem; color:var(--cyan); margin-bottom:3px; }
  .node.active + .node-label{ opacity:1; transform:translate(-50%, -160%); }

  .map-hint{ margin-top:18px; font-size:.8rem; color:rgba(243,236,255,.55); text-align:center; }

  /* ---------- fullscreen toggle ---------- */
  #fs-toggle{
    position:fixed; top:20px; left:20px; z-index:50;
    width:44px; height:44px; border-radius:50%;
    display:flex; align-items:center; justify-content:center;
    cursor:none; color:var(--mist);
  }
  #fs-toggle svg{ width:18px; height:18px; }

  #brand{
    position:fixed; top:24px; left:78px; z-index:50;
    font-family:var(--font-display); font-weight:700; font-size:.85rem;
    letter-spacing:.12em; color:rgba(243,236,255,.75);
  }

  @media (max-width: 640px){
    #nav{ right:10px; gap:10px; }
    #brand{ display:none; }
    .gallery-grid{ grid-template-columns:repeat(2, 1fr); max-height:64dvh; }
  }

  @media (prefers-reduced-motion: reduce){
    *, *::before, *::after{ animation-duration:.01ms !important; animation-iteration-count:1 !important; transition-duration:.01ms !important; }
  }